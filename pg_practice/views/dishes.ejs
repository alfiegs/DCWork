<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <% dishes.forEach(record => { %>
        <h1><%= record.name %> </h1>
        <h2><%= record.description %> </h2>
        <h3><%= record.price %> </h3>
        <% if (record.imageURL) { %>
        <img src="<%= record.imgURL %>" alt="">
        <% } %>
        <% else { %>
            <img src="https://assets3.thrillist.com/v1/image/2797371/size/tmg-article_default_mobile.jpg" height="200px" alt="">
            <% } %>
    <% }) %>

    <!-- WHAT WE ARE DOING ABOVE -->
    <!-- dishes.forEach(record => {
        <h1>record.name</h1>
        if(record.imgURL){
            <img src="record.imgURL" alt="">
        }
        else{
            <img src="default source" alt="">
        }
    }) -->
</body>
</html>